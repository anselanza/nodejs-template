{
  "name": "app",
  "version": "0.4.0",
  "description": "An opinionated NodeJS project template",
  "main": "dist/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npx tsc",
    "postinstall": "npm run build && git init",
    "start": "npx tsx src/index.ts",
    "dev": "npx tsx --watch src/index.js --loglevel=debug"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "log4js": "^6.3.0",
    "parse-strings-in-object": "^1.6.0",
    "rc": "^1.2.8"
  },
  "devDependencies": {
    "@tsconfig/node24": "^24.0.4",
    "@types/node": "^24.12.4",
    "@types/rc": "^1.1.0",
    "prettier": "^2.2.1",
    "tsx": "^4.22.4",
    "typescript": "^6.0.3"
  }
}
