{
  "name": "app",
  "version": "0.3.0",
  "description": "",
  "main": "dist/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npx tsc",
    "postinstall": "npm run build && git init",
    "start": "npm run build && node dist/index.js",
    "dev": "npm run build && node dist/index.js --loglevel=debug"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "log4js": "^6.3.0",
    "parse-strings-in-object": "^1.6.0",
    "rc": "^1.2.8"
  },
  "devDependencies": {
    "@types/node": "^14.14.37",
    "@types/rc": "^1.1.0",
    "prettier": "^2.2.1",
    "typescript": "^4.7"
  }
}
